Got it. You want a **Replit prompt** that tells Replit AI to take your old project code and make all the changes we manually did so it becomes deployable online.

Copy-paste this into Replit AI:

---

## PROMPT FOR REPLIT AI — MAKE MY EXISTING WEBSITE PRODUCTION READY

I have an existing e-commerce website project. Do **not rebuild it from scratch**. Modify my current code and preserve all existing features.

The goal is to make this website fully functional online with separate frontend, backend, database, and image hosting.

### Current project goal:

Convert this local/Replit website into a production-ready application.

### Required architecture:

**Frontend**

* React + Vite
* Deployable on Netlify

**Backend**

* Node.js + Express API
* Deployable on Render

**Database**

* PostgreSQL database (Render PostgreSQL)
* Replace any local SQLite dependency

**Images**

* Replace local image uploads with Cloudinary
* Product images must remain available after deployment/restarts

---

## Backend changes required:

### 1. PostgreSQL migration

Replace SQLite database code with PostgreSQL.

Keep existing tables and functionality:

Products table should support:

* id
* name
* category
* description
* price
* stock
* image_url
* images (array/json)
* featured
* created_at

Admins table should support:

* id
* email
* password_hash

Use environment variables:

```
DATABASE_URL
ADMIN_EMAIL
ADMIN_PASSWORD
JWT_SECRET
```

---

### 2. Cloudinary image upload

Remove local upload storage.

Do NOT use:

```
uploads/
express.static('/uploads')
multer.diskStorage()
```

Use:

```
cloudinary
multer-storage-cloudinary
```

Configure:

```
CLOUDINARY_CLOUD_NAME
CLOUDINARY_API_KEY
CLOUDINARY_API_SECRET
```

Product uploads should support multiple images:

```
upload.array('images', 10)
```

Save Cloudinary URLs directly into the database.

---

### 3. Keep admin functionality working

Maintain:

```
POST /api/auth/login
GET /api/products
GET /api/products/:id
GET /api/admin/products
POST /api/admin/products
PUT /api/admin/products/:id
DELETE /api/admin/products/:id
```

Admin authentication must continue using JWT.

---

## Frontend changes required:

Make frontend work correctly with deployed backend.

Use environment variable:

```
VITE_API_URL
```

instead of hardcoded localhost URLs.

Example:

```
http://localhost:5000
```

should become:

```
https://my-render-backend-url.onrender.com
```

---

## Image handling fix:

The frontend must support both:

Old local images:

```
/uploads/image.jpg
```

and Cloudinary images:

```
https://res.cloudinary.com/....jpg
```

Never add API URL before Cloudinary URLs.

Use this logic everywhere images are displayed:

```javascript
const imgSrc = image
 ? (image.startsWith('http')
     ? image
     : `${api.base}${image}`)
 : null;
```

Apply this to:

* ProductCard
* Product details page
* Product gallery
* Admin product preview

---

## Deployment preparation:

### Backend:

Prepare for Render:

* Add correct start script
* Use PORT from environment:

```javascript
process.env.PORT || 5000
```

* Enable CORS for frontend URL.

---

### Frontend:

Prepare for Netlify:

* Build command:

```
npm run build
```

* Output folder:

```
dist
```

Add React routing support:

Create:

```
public/_redirects
```

with:

```
/* /index.html 200
```

so routes like:

```
/admin/login
/product/:id
```

work after refresh.

---

## Important instructions:

* Do not remove existing UI.
* Do not change the design unnecessarily.
* Do not rename existing components unless required.
* Give me a list of files changed.
* Before modifying, inspect the current project structure.
* Fix errors instead of creating duplicate code.
* Keep the admin panel working.
* Make the final project ready for:

  * GitHub
  * Render
  * Netlify
  * Cloudinary
  * PostgreSQL

---

After completing changes, show:

1. Updated file list
2. Environment variables needed
3. Deployment steps

---

This prompt should make Replit modify your old code similarly to what we did manually.
